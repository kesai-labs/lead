{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Reset Carla World",
            "type": "shell",
            "command": "python scripts/reset_carla_world.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            }
        }
    ]
}
